# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Data and cached artifacts (activations are hundreds of MB; re-derive, don't commit)
# .gitkeep survives so the layout exists on a fresh clone
activations/*
data/raw/*
artifacts/*
!activations/.gitkeep
!data/raw/.gitkeep
!artifacts/.gitkeep
*.pt
*.npz
*.safetensors

# Secrets
.env
.env.*

# OS
.DS_Store

# Planning docs stay local
.scratch/
.planning/
plans/
PLAN.md
SPEC.md
*.plan.md

# Agentic development config stays local
CLAUDE.md
AGENTS.md
.claude/
docs/agents/
docs/adr/
CONTEXT.md
CONTEXT-MAP.md
